<?xml version="1.0" encoding="utf-8"?>
<!-- Drawn white on purpose: the provider tints it with the state tone, so one
     shape carries every state without a drawable per colour. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">
    <solid android:color="#42FFFFFF" />
</shape>
